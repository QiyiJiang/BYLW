random_seed: 42

retrieval:
  topk_list: [1, 3, 5]

qa:
  default_topk: 3
  max_context_tokens: 24000
  max_answer_tokens: 1024
  temperature: 0.0

ablation:
  colqwen_topk_variants: [1, 3, 5]

backend:
  vector_store: "milvus"  # 当前仅向量检索走 milvus-lite，BM25 仍使用 rank-bm25 本地实现

